import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // --- Couleurs Principales & Palette Marque ---
  /// Couleur principale de l'application (Violet Royal)
  static const Color primary = Color(0xFF763CD1);

  /// Nuance claire de la couleur principale
  static const Color primaryLight = Color(0xFF9862E8);

  /// Couleur d'accent (Jaune Ambré chaleureux)
  static const Color accent = Color(0xFFF8B727);

  /// Couleur secondaire (Bleu Ciel dynamique)
  static const Color secondary = Color(0xFF358CED);

  /// Couleur Tertiaire (Vert Menthe / Turquoise)
  static const Color teal = Color(0xFF39C0AD);

  /// Couleur Quaternaire (Indigo Sombre / Violet Profond)
  static const Color indigo = Color(0xFF422B95);

  // --- Couleurs Sémantiques ---
  /// Succès / Validation (Vert Menthe)
  static const Color success = Color(0xFF39C0AD);

  /// Avertissement / Attention (Jaune Ambré)
  static const Color warning = Color(0xFFF8B727);

  /// Information (Bleu Ciel)
  static const Color info = Color(0xFF358CED);

  /// Danger / Erreur (Rouge Corail)
  static const Color danger = Color(0xFFEF4444);

  // --- Neutres & Arrière-plans ---
  /// Fond principal avec une touche violette très douce
  static const Color background = Color(0xFFF8F7FC);

  /// Fond des cartes et conteneurs
  static const Color surface = Colors.white;

  /// Variante de fond pour cartes en évidence
  static const Color surfaceVariant = Color(0xFFF1EEFA);

  /// Texte principal (Indigo sombre haute lisibilité)
  static const Color textPrimary = Color(0xFF1E1B2E);

  /// Texte secondaire et sous-titres
  static const Color textSecondary = Color(0xFF6C687D);

  /// Bordures et séparateurs
  static const Color border = Color(0xFFE6E2F2);

  /// Icônes
  static const Color icon = Color(0xFF6C687D);

  /// Élément désactivé
  static const Color disabled = Color(0xFFD5D0E6);

  static const Color white = Colors.white;
  static const Color black = Color(0xFF111827);
}

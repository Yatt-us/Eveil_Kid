import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // ============================================================
  // COULEURS PRINCIPALES — PARENT / VISITEUR / MANAGER
  // ============================================================

  /// Couleur principale de l'application
  static const Color primary = Color(0xFF763CD1);

  /// Nuance claire de la couleur principale
  static const Color primaryLight = Color(0xFF9862E8);

  /// Couleur d'accent
  static const Color accent = Color(0xFFF8B727);

  /// Couleur secondaire
  static const Color secondary = Color(0xFF358CED);

  /// Couleur tertiaire
  static const Color teal = Color(0xFF39C0AD);

  /// Indigo sombre
  static const Color indigo = Color(0xFF422B95);

  // ============================================================
  // COULEURS SÉMANTIQUES
  // ============================================================

  /// Succès / Validation
  static const Color success = Color(0xFF39C0AD);

  /// Avertissement / Attention
  static const Color warning = Color(0xFFF8B727);

  /// Information
  static const Color info = Color(0xFF358CED);

  /// Erreur / Suppression / Danger
  static const Color danger = Color(0xFFEF4444);

  // ============================================================
  // NEUTRES — THÈME CLAIR
  // ============================================================

  /// Fond principal
  static const Color background = Color(0xFFF8F7FC);

  /// Fond des cartes et conteneurs
  static const Color surface = Colors.white;

  /// Variante de surface
  static const Color surfaceVariant = Color(0xFFF1EEFA);

  /// Texte principal
  static const Color textPrimary = Color(0xFF1E1B2E);

  /// Texte secondaire
  static const Color textSecondary = Color(0xFF6C687D);

  /// Bordures et séparateurs
  static const Color border = Color(0xFFE6E2F2);

  /// Icônes
  static const Color icon = Color(0xFF6C687D);

  /// Éléments désactivés
  static const Color disabled = Color(0xFFD5D0E6);

  // ============================================================
  // NEUTRES — THÈME SOMBRE
  // ============================================================

  /// Fond principal sombre
  static const Color darkBackground = Color(0xFF12101A);

  /// Surface sombre
  static const Color darkSurface = Color(0xFF1D1928);

  /// Surface sombre secondaire
  static const Color darkSurfaceVariant = Color(0xFF292336);

  /// Texte principal sombre
  static const Color darkTextPrimary = Color(0xFFF8F7FC);

  /// Texte secondaire sombre
  static const Color darkTextSecondary = Color(0xFFB8B3C7);

  /// Bordure sombre
  static const Color darkBorder = Color(0xFF3A3348);

  /// Icône sombre
  static const Color darkIcon = Color(0xFFB8B3C7);

  /// Élément désactivé sombre
  static const Color darkDisabled = Color(0xFF5C5668);

  // ============================================================
  // THÈME ENFANT — VERT
  // ============================================================

  /// Couleur principale de l'espace enfant
  static const Color childPrimary = Color(0xFF22C55E);

  /// Variante claire du vert enfant
  static const Color childPrimaryLight = Color(0xFF4ADE80);

  /// Variante sombre du vert enfant
  static const Color childPrimaryDark = Color(0xFF16A34A);

  /// Couleur secondaire enfant
  static const Color childSecondary = Color(0xFF10B981);

  /// Fond de l'espace enfant
  static const Color childBackground = Color(0xFFF0FDF4);

  /// Surface enfant
  static const Color childSurface = Colors.white;

  /// Variante de surface enfant
  static const Color childSurfaceVariant = Color(0xFFDCFCE7);

  /// Texte principal enfant
  static const Color childTextPrimary = Color(0xFF14532D);

  /// Texte secondaire enfant
  static const Color childTextSecondary = Color(0xFF4B5563);

  /// Bordure enfant
  static const Color childBorder = Color(0xFFBBF7D0);

  // ============================================================
  // COULEURS COMMUNES
  // ============================================================

  static const Color white = Colors.white;

  static const Color black = Color(0xFF000000);

  static const Color transparent = Colors.transparent;
}

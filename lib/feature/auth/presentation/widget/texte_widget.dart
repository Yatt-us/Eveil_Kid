import 'package:flutter/material.dart';

class TexteWidget extends StatelessWidget {
  const TexteWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}

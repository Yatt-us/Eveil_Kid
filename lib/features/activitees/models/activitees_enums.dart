enum CategorieActivitee { math, lecture, logique, sciences, langues }

enum DifficulteActivitee { facile, moyen, difficile }

enum StatutActivitee { toutes, enCours, terminees }

enum TypeAffichageQuestion { liste, grille }